name: tool-guard
version: 0.1.0
description: Pre-tool-call security guard for Hermes Agent with allow / approval-needed / block policy decisions.
provides_hooks:
  - pre_tool_call
  - post_tool_call
  - pre_approval_request
  - post_approval_response
